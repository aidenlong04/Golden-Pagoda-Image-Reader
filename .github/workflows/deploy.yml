name: Deploy to Hetzner

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-hetzner
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.HETZNER_SSH_KEY }}" > ~/.ssh/hetzner
          chmod 600 ~/.ssh/hetzner
          ssh-keyscan -H "${{ secrets.HETZNER_HOST }}" >> ~/.ssh/known_hosts

      - name: Sync repo to server
        run: |
          rsync -az --delete \
            -e "ssh -i ~/.ssh/hetzner" \
            --exclude '.git' \
            --exclude '.github' \
            --exclude '__pycache__' \
            --exclude '*.pyc' \
            --exclude '.venv' \
            --exclude '.env' \
            ./ "root@${{ secrets.HETZNER_HOST }}:/opt/golden-pagoda/"

      - name: Build image and restart service
        run: |
          ssh -i ~/.ssh/hetzner "root@${{ secrets.HETZNER_HOST }}" '
            set -e
            cd /opt/golden-pagoda
            install -m 0644 scripts/golden-pagoda.service /etc/systemd/system/golden-pagoda.service
            systemctl daemon-reload
            docker build -t golden-pagoda:latest . | tail -3
            systemctl restart golden-pagoda
            sleep 5
            systemctl is-active golden-pagoda
            docker logs golden-pagoda --tail 20
          '
